import SalesPipelinePredictor from '@/components/SalesPipelinePredictor';

export default function Home() {
  return (
    <main style={{ backgroundColor: '#f3f4f6', minHeight: '100vh' }}>
      <SalesPipelinePredictor />
    </main>
  );
}
